<h1>Adherents</h1>
<table class="table table table-bordered table table-hover">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Nom</th>
      <th scope="col">Prénom</th>
    
      <th scope="col">Show</th>
      <th scope="col">Edit</th>
    <!--  <th scope="col">Destroy</th>-->
    </tr>
  </thead>
  <tbody>
  	 <% @adherents.each do |adherent| %>
    <tr>
      <th scope="row"><%= adherent.id %></th>
      <td><%=adherent.nom %></td>
      <td><%=adherent.prenom %></td>
      
      <td>
      <%= link_to "Show", adherent_path(adherent) %>
      </td>
      <td>
      <%= link_to "Edit", edit_adherent_path(adherent) %>
      </td>
      <!--
      <td>
      <%= link_to "Destroy", adherent_path(adherent), method: :delete, data: {confirm: "Are you sure ?"} %>
      </td> -->
    </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "New adherent", new_adherent_path ,class:"btn btn-outline-primary" %>








